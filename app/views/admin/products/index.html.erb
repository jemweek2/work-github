<div class="container">
<div class="row mt-4 ml-4">
<div>
<h3 class="col bg-secondary text-left">商品一覧</h3>
</div>
<div class="col text-right">
   <%= link_to image_tag("btn.png", :size => '50x50'),new_admin_product_path, class:"mr-3"%>
</div>

</div>
<div class="row mt-2 mx-5">
<table class="table", align="center",border="1" cellpadding="6" cellspacing="0">
  <thead style="background:skyblue">
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
      <th>ジャンル</th>
      <th>ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @product.each do |product| %>
    <tr>
      <th><%= product.id %></th>
      <td><%= link_to product.name, admin_product_path(product) %></td>
      <td><%= product.no_tax_price.to_s(:delimited) %></td>
      <td><%= product.genre.name %></td>
      <td><% unless product.is_active %><p class="text-muted">販売停止中</p>
          <% else %><p class="text-success">販売中</p>
          <% end %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
<div class="row">
<%= paginate @product %>
</div>
</div>
