  <div class="container-fluid" >
  <div class="row mt-4 mx-2">
    <%= render 'layouts/genrebar' %>
    <div class="col-md-9">
      <div class="row">
        <div class="col-md-4" id="img_field">
          <%= image_tag @product.product_image, size: "300x300" %>
        </div>

        <div class="col-md-8">
        <h3><%= @product.name %></h3>
        <br>
        <p><%= @product.description %></P>
        <br>

        <p>￥<%= @product.add_no_tax_price.to_s(:delimited) %>(税込)</p>
        </div>
      </div>
  </div>
</div>